# Backend Setup

## Prerequisites
- Node.js 18+
- MongoDB

## Run
1. Copy `.env.example` to `.env`
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start development server:
   ```bash
   npm run dev
   ```

API docs: `src/docs/api.md`
